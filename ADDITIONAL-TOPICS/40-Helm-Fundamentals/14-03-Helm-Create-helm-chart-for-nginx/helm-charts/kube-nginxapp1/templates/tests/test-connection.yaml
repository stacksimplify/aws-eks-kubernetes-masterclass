apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-nginxapp1.fullname" . }}-test-connection"
  labels:
    {{- include "kube-nginxapp1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-nginxapp1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
