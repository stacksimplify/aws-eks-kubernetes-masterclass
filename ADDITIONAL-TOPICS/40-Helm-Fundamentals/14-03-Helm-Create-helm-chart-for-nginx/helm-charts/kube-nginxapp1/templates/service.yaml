apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-nginxapp1.fullname" . }}
  labels:
    {{- include "kube-nginxapp1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-nginxapp1.selectorLabels" . | nindent 4 }}
